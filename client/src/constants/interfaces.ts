// Nav Bar language interface
export interface ILanguage {
  id: number;
  label: string;
}
export interface Subscription {
  id: string;
  type: string;
  startedOn: string;
  price: string;
  access: string;
  billingDate: string;
  active:boolean;
}


